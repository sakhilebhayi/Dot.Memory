---
title: Dot.Memory — Platform Wiki
version: 0.6.0
status: draft
owners: [Memory Platform Lead]
platform-id: dot-memory
last-review: 2026-08-07
---

# Dot.Memory

Purpose: this is Dot.Memory's own knowledge home — owned and maintained by the Dot.Memory team. It describes what this platform is, what it stores, and how it connects to the wider Dot Ecosystem. Dot.Brain never edits this file; it only reads what we choose to publish.

> **Related:** [Dot.Brain's ingested view of this platform](https://github.com/sakhilebhayi/Dot.Brain/blob/main/platforms/dot-memory.md) · [Dot.Brain's Memory Orchestration spec](https://github.com/sakhilebhayi/Dot.Brain/blob/main/brain.memory.md)

---

## 1. What Dot.Memory Is

Dot.Memory is the persistence substrate for the Dot Ecosystem: knowledge-graph storage, vector indexes for semantic retrieval, Knowledge Pack archives, and the audit trails that every other platform's governance gates write to. It is infrastructure, not a knowledge consumer — Dot.Memory stores content without reading it.

**Status:** early-stage, now with a first hand-authored code pass. This repository contains a
Jetstream Teams application shell plus the domain models, migrations, dashboard, and seeder
described in §3–§5 below — but it is **unverified**: it was written in an environment without
PHP, Composer, or PostgreSQL, so nothing here has actually been installed, migrated, or run. Treat
every section below as the current design intent that the code *attempts* to follow, not proven
shipped behavior, until a real `composer install && php artisan migrate && php artisan test` pass
confirms it and the change log is updated accordingly.

## 2. Design Principle: Store Without Reading

The one rule that shapes everything else here: **Dot.Memory the infrastructure stores knowledge without reading it; Dot.Memory the platform publishes only its own operational telemetry** (latency, durability, integrity — never content). A storage platform that mined its tenants' knowledge would be the ecosystem's deepest possible trust violation, so the telemetry plane and the data plane are architecturally separate, not just policy-separate.

## 3. Storage Tiers

| Tier | Contents | Latency target | Backing |
|---|---|---|---|
| Hot | Actively referenced graph nodes/edges, verified lessons, registries | ≤ 50 ms p95 (per-op) | Graph store + cache projections |
| Warm | Unreferenced 90 days–2 years, provisional conclusions, dormant edges | ≤ 2 s p95 | Warm store |
| Cold | Superseded/retracted knowledge, full ledger history | ≤ 5 min (async) | Immutable cold archive |

Promotion is demand-driven; demotion is policy-driven and logged. Nothing skips Cold to deletion — see §6.

## 4. Entities We Own

| Entity | Natural key | Notes |
|---|---|---|
| Storage tier | tier ID | hot / warm / cold |
| Index | index ID + version | graph, vector, and audit-log indexes |
| Retrieval class | `retr:<consumer-class>:<tier>` | the unit of our SLA contract, §5 |
| Retrieval observation | class × tier × window | latency/failure aggregates — telemetry only |
| Durability outcome | audit period | verified restore tests, integrity checks |

Stored content itself is never modeled as our entity — it belongs to whichever platform published it.

## 5. Retrieval SLA Contract

Four named, consumer-visible contracts:

| Class | Serves | Contract | On breach |
|---|---|---|---|
| `retr:agent-context:hot` | Colony agents assembling working context | p95 ≤ 800 ms, p99 ≤ 2 s | Degraded-mode flag: agents disclose stale-context risk |
| `retr:surface:hot` | Human-facing surfaces | p95 ≤ 1.5 s | Serve cached-with-timestamp instead of blocking |
| `retr:audit:warm` | Governance/audit-log access | p95 ≤ 30 s, completeness guaranteed | Escalate — audit failure is a governance event |
| `retr:archive:cold` | Compliance retrieval | ≤ 24 h, zero-loss | Integrity incident, mandatory pack |

How retrieval fails is specified, not improvised — degraded-mode behavior is part of the contract, not an afterthought.

## 6. Forgetting Policy

We never delete knowledge; we let it lose salience:

1. **Supersede** — the only mutation; superseded versions demote toward Cold with the chain intact.
2. **Dormancy** — confidence decay stops a node from being served by context retrieval, but it stays retrievable by provenance.
3. **Expiry** — `valid_until` passing behaves like dormancy and flags the node for a successor observation.
4. **Never-forget set** — verified lessons, the ledger, and hard-invariant evidence are exempt from demotion below Hot/Warm.
5. **Legal erasure** — the one real tension with never-delete; standing design is crypto-shredding (destroy the key, not the record) — see Dot.Brain's [ADR-0009](https://github.com/sakhilebhayi/Dot.Brain/blob/main/adr/ADR-0009-crypto-shredding-legal-erasure.md).

## 7. Events We Emit

| Event | Trigger | Frequency |
|---|---|---|
| `memory.sla.breach` | A retrieval class misses its contract | target: 0 |
| `memory.tier.migration_completed` | Content ages between tiers | daily cycles |
| `memory.integrity.check_completed` | Scheduled durability verification | weekly |

## 8. Connecting to Dot.Brain

Dot.Memory participates in the ecosystem as a registered platform (`dot-memory`) that publishes Knowledge Packs about its own operation — never about the content it stores.

| Payload type | Cadence | Contains |
|---|---|---|
| `observation` | weekly | retrieval-performance aggregates |
| `insight` | per finding | access-pattern *shapes* only — never query text or result content |
| `outcome` | per period | SLA and durability verification results |
| `incident` | per incident | SLA breaches, integrity events |

We subscribe to Dot.Brain recommendations on index strategy, tier-policy tuning, and capacity forecasting. Full manifest, entity/event mapping, and a worked publish→PR round-trip are maintained on the Brain side at [`platforms/dot-memory.md`](https://github.com/sakhilebhayi/Dot.Brain/blob/main/platforms/dot-memory.md) — that document is Dot.Brain's ingested view and is authoritative for integration mechanics; this wiki is authoritative for what Dot.Memory actually *is*.

## 9. Roadmap

- [x] Scaffold the Jetstream Teams application shell (auth, profile, 2FA, API tokens, ecosystem SSO) — **unverified, not yet run**
- [x] Model storage tiers, indexes, retrieval classes, retrieval observations, and durability outcomes as Eloquent models + migrations — **unverified, not yet migrated**
- [x] Build a first SLA-attainment / index-inventory / durability-outcomes monitoring dashboard — **unverified, not yet rendered**
- [x] Seed realistic example telemetry matching the four SLA classes' targets — **unverified, not yet seeded**
- [ ] Actually install, migrate, and test the above against a real PHP/Postgres environment
- [ ] Stand up the real graph store and vector index (hot tier) — the current app only tracks index *metadata*, not a real index
- [ ] Wire the four retrieval SLA classes to real degraded-mode behavior (the schema models the contract; nothing yet measures live traffic against it)
- [ ] Publish the first `observation` Knowledge Pack (hello-pack per Dot.Brain's onboarding procedure)
- [ ] Warm/cold tiering automation
- [ ] Crypto-shredding implementation for legal erasure

## Change Log

| Version | Date | Author | Change |
|---|---|---|---|
| 0.6.0 | 2026-08-07 | Platform-loop pass | Ecosystem-wide standardization pass: branded transactional email theme, POPIA-aligned legal pages, and hero-styled auth pages, matching the pattern piloted on InfoDot and already rolled out to 14 sibling platforms. **Legal pages:** enabled `Features::termsAndPrivacyPolicy()` in `config/jetstream.php` (previously commented out, per the 0.5.3 entry's own note that `policy.blade.php`/`terms.blade.php` existed, pre-restyled, but were unreachable). Created `resources/markdown/{policy,terms,cookies}.md` — this repo had no `resources/markdown/` directory at all — naming **BluePin Inc** as the responsible party, aligned with South Africa's POPIA. Structured around this platform's own "store without reading" trust boundary (§2/§4): BluePin is the **responsible party** only for account holders who sign in to monitor the SLA/index/durability dashboard; for content other platforms store here, BluePin is at most an **operator**, since Dot.Memory's domain models have no field for the content itself and no way to associate it with an individual — the Policy states plainly that questions about specific stored content go to the platform that generated it. Documents the real forgetting policy from §6 (supersede/dormancy/expiry, never outright delete) and names crypto-shredding (per Dot.Brain's ADR-0009) as the standing design intent for legal erasure, honestly flagged as not yet implemented. Added a hand-wired `/cookies` route reading the new `cookies.md`, grounded in the real session cookie name (`dot-memory-session`, confirmed in `config/session.php`). `policy.blade.php`/`terms.blade.php` already existed with correct dark-theme styling (`prose dark:prose-invert`) from the 0.5.3 pass and needed no changes beyond becoming reachable; added a matching `cookies.blade.php`. Footer of `welcome.blade.php` got Privacy/Cookies/Terms links, additive alongside the existing logo/copyright row. **Auth hero:** per the 0.5.3 entry's own reasoning (re-confirmed here, not re-litigated), this platform's welcome page deliberately has no photo anywhere — Dot.Memory is infrastructure with no photographable subject, and its real visual identity is a radial amber glow over `--ink` plus a purpose-drawn line-art "memory card + graph nodes" signature SVG. Brought that exact treatment (same gradient, same SVG, same opacity) to `components/authentication-card.blade.php`'s full-screen wrapper, which previously had a flat `--ink` background with neither. Confirmed the platform's permanent `.dark`-class/`!important`-override styling (documented as intentional in the 0.5.3 entry, not a leftover toggle) needed no changes — the new elements use the same `--ink`/`--amber`/`--line` tokens already in scope. Verified across `/login` at desktop (1600px, signature SVG visible per its own `hidden lg:block`) and mobile (375px, SVG correctly hidden, card fully legible, no overflow). **Branded transactional email theme:** published Laravel's markdown mail vendor views for the first time in this repo and rewrote both `message.blade.php` (real `<img>` logo, hardcoded "Dot.Memory"/"Persistence substrate for the Dot Ecosystem." footer) and `themes/default.css`. Chose a dark `--ink` header band over a light `--paper` body — the same pairing every sibling platform's theme uses — rather than a fully dark email body, since email clients render dark templates inconsistently; this is a deliberate divergence from the live app's own permanent dark theme, not an oversight. This platform's palette has no pre-existing "-deep" amber token for link/hover contrast (`guest.blade.php` only defines `--amber`/`--amber-soft`/`--gold`), so computed a new AA-contrast-safe `#a66d08` for body-text links, following the same "-deep" pattern every sibling platform's theme already uses for its own accent. Verified via a temporary `/_mail-preview` route rendering `ResetPassword::toMail()` — dark header with logo, gold button with correct dark-ink text, light legible body, branded footer — removed before commit. **Verification:** `npm run build` clean; `php artisan config:clear && route:clear && view:clear`; full suite green, 14/14 tests, 0 failed, unchanged from the 0.5.3 baseline; browser-verified `/login` and `/privacy-policy` at desktop and mobile widths, zero console errors. |
| 0.5.3 | 2026-08-06 | Platform-loop pass | **Hero photo: deliberately skipped.** Dot.Memory is the ecosystem's persistence substrate — knowledge-graph storage, vector indexes, Knowledge Pack archives (wiki.md §1) — with no real-world photographable subject; the 0.4.0 pass actually shipped a stock server-rack photo here and the 0.5.2 pass *removed* it in favor of a purpose-drawn line-art signature element (the memory-card + graph-node silhouette) specifically because a generic server-room photo didn't represent what this platform does any better than the illustration. Re-confirmed that reasoning rather than re-adding a photo for its own sake. **Redesigned the full Jetstream/Fortify auth page family** to match the welcome page's real brand tokens instead of the stock centered-white-card-on-gray-100 scaffold: `resources/views/layouts/guest.blade.php` now loads the same Space Grotesk/IBM Plex Sans/IBM Plex Mono fonts and the identical `:root` CSS custom properties (`--ink #12141a`, `--amber #f2a70b`, `--paper #eef1f5`, `--mist #9aa4b6`, `--line`) as `welcome.blade.php`, and forces the `.dark` class permanently (guest pages now render in the platform's single dark brand palette with no light/dark toggle, matching the welcome page, which has none either — the removed toggle only ever affected guest pages, so the authenticated dashboard's own layout/toggle is untouched). Rather than editing the shared `x-input`/`x-label`/`x-button`/`x-checkbox` components (used across the authenticated app too, out of scope for a guest-facing pass), added scoped `!important` CSS overrides inside `guest.blade.php` targeting the exact Tailwind utility classes those components already emit (`dark:bg-gray-900` → `--ink-soft`, `dark:text-gray-400` → `--mist`, indigo focus rings → `--amber`, the button's `dark:bg-gray-200` reversed-color treatment → `--amber`/`#12141a`) — this stylesheet only ever loads via the guest layout, so it cannot leak into the dashboard, and no component `name`/`action`/`route()`/`@csrf`/`@error` binding was touched. Rebuilt `components/authentication-card.blade.php` as a quiet branded frame (logo above a `--ink-soft` card, `--line` border, on an `--ink` page background, replacing `bg-gray-100`/`bg-white`) and bumped `components/authentication-card-logo.blade.php` from the undersized default `size-16` to `h-14 sm:h-[4.5rem]` — the exact height classes already proven legible on the welcome page's own nav logo (the same `dfc4547`-lesson class pair used ecosystem-wide). Added a short `font-display` heading to each of login/register/forgot-password/reset-password/verify-email/confirm-password/two-factor-challenge (none had any heading before) and restyled `terms.blade.php`/`policy.blade.php` onto the same frame — both are present but currently unreachable in this app, since `Features::termsAndPrivacyPolicy()` is commented out in `config/jetstream.php`; restyled anyway so they're ready if the feature is ever enabled. Also confirmed `verify-email.blade.php` is likewise currently unreachable (`App\Models\User` does not implement `MustVerifyEmail`) but restyled it too for the same reason. Left the two-factor-challenge page's Alpine `x-data`/`x-show`/`x-cloak` recovery-code toggle logic untouched — a pre-existing gap (this repo has no `alpinejs` dependency at all, confirmed via `package.json`/`node_modules`, the same finding the 0.5.2 welcome-page pass made) that's a functional bug outside this visual-only pass's scope, not something introduced here. Verified for real: `npm run build` clean; created and migrated an isolated local `dot_memory_verify` PostgreSQL 16 database; `php artisan serve` preview at 375px and 1600px (no horizontal overflow, no console errors) via the Browser tool for `/login`, `/register`, `/forgot-password`, and `/reset-password/{token}` (dummy token — Fortify's `NewPasswordController@create` doesn't validate token existence on GET, so this renders safely without a real reset flow); a throwaway local-only test user (`loop-verify@example.test`, deleted after verification, mail driver is `log` so nothing was actually sent) was created via `tinker` to exercise `/login` → `/user/confirm-password` and, after enabling 2FA on that same throwaway user, `/login` → `/two-factor-challenge`, both confirmed via authenticated `curl` requests to render 200 with the new heading text present and no PHP exception/error markers in the response body. |
| 0.5.2 | 2026-08-06 | Platform-loop pass | Redesigned `resources/views/welcome.blade.php`, following the ecosystem's guest-page pattern piloted on Dot.Mines (`d191d10`/`dfc4547`). The 0.4.0 page was the generic dark-SaaS template the pilot warns against — near-black `#0b0b10` background, a single bright-violet accent (`#8b5cf6`, unrelated to the real brand), a centered hero, and a 3-to-4-equal-icon-card feature grid. Sampled the real logo (`public/images/logo.png`) directly: a memory-card icon in mustard-gold/amber on a two-tone gold-to-amber lockup — grounded the new palette in those actual colors instead of the arbitrary violet (`--ink #12141a` — a cool near-black distinct from Mines' warm-brown `--ink`, `--amber #f2a70b`, `--gold #f4c94c`, `--paper #eef1f5`, plus a small `--cold #7b93b8` slate-blue used only in the storage-tiers table to represent the Cold tier, since this platform's own domain concept is a warm→cold gradient). Typography: `Space Grotesk` (display) + `IBM Plex Sans` (body) + `IBM Plex Mono` (data/labels) — deliberately not Mines' Outfit/Plus Jakarta Sans/JetBrains Mono pairing, and not Inter. Replaced the centered hero and equal-card grid with an asymmetric hero (copy left, signature element right) and a divided-list feature section (hairline borders, mono tags) mirroring Mines' structural pattern with Dot.Memory's own six real capabilities (tiers, SLA contracts, indexes, forgetting policy, durability, trust boundary) drawn from wiki.md §3–§7. Signature element: a large, quiet line-art memory-card silhouette with graph nodes/edges traced across it — echoing both the logo's own SD-card icon and the "knowledge graph" it stores, the platform's equivalent of Mines' headframe silhouette. The real storage-tiers table (wiki.md §3) was kept and restyled to the new palette rather than replaced, since it's already grounded, factual content. Copy audited against wiki.md §1/§3/§5 — no fabricated stats; the hero's instrumentation strip lists the four real named SLA classes and their actual contracts (p95 ≤800ms, etc.), not invented numbers. No dead `href="#"` links were present or introduced — all nav/footer links resolve to real in-page anchors or `route('login')`/`route('register')`. Motion: single restrained `IntersectionObserver` scroll-reveal + `scale(0.97)` press-feedback, `prefers-reduced-motion` respected, no perpetual animation. Rebuilt the nav/mobile-menu with plain vanilla JS instead of Alpine `x-data` — this repo's `package.json` has no `alpinejs` dependency and `resources/js/app.js` is empty, so the Mines pattern's Alpine directives would have silently no-opped on this guest page (`x-data`/`x-show`/`@click` require Alpine's `window.Alpine` global, which is never loaded outside authenticated Livewire pages here); confirmed via DOM inspection that mobile-menu toggle and scroll-triggered nav background now work correctly. **Logo legibility, the exact `dfc4547` lesson**: nav logo set to `h-14`/`h-[4.5rem]` (56px mobile / 72px sm+, this lockup's more compact square-ish proportions read clearly below Mines' 64–80px) and footer to `h-11` (44px, matching Mines). Verified end-to-end: `npm install && npm run build` clean; local `php artisan serve` preview (session driver overridden to `array` for the preview process only, since this platform's Postgres session-store database doesn't exist locally per the 0.5.0/0.5.1 entries' "unverified" caveat — `.env` itself was not modified) at desktop (1280px), narrow (500px), and mobile (375px) viewports via the Browser tool; confirmed via `getBoundingClientRect()` the logo is 72px/56px tall at each breakpoint, no horizontal overflow at 375px, hero text renders at full contrast (`rgb(238,241,245)` on `rgb(18,20,26)`), the reveal animation reaches `opacity:1`/`is-visible`, and the mobile menu toggle opens/closes correctly. All nav/footer/CTA hrefs confirmed real (`curl`-inspected rendered HTML: `/login`, `/register`, `#features`, `#tiers` — no `href="#"` placeholders). |
| 0.5.1 | 2026-08-04 | Platform-loop pass | **Unguarded-null-actor audit (the Dot.Mines `0cc4362` `resolveCurrentTeam()` bug pattern, adapted to a repo with no team concept).** Grepped every `app/Livewire/*.php`, `app/Http/Controllers/**/*.php`, plus confirmed `app/Jobs`, `app/Listeners`, and `app/Console/Commands` **do not exist in this repo at all** — there are no queued jobs, event listeners, or console commands to audit. Across the whole `app/` tree, exactly one file dereferences `Auth::user()`/`auth()->user()`: `app/Livewire/NotificationBell.php` (`notifications()`, `unreadNotifications()`, `markAsRead()`, `markAllAsRead()` — all four call `auth()->user()->` unguarded). Traced its only mount point: `resources/views/layouts/app.blade.php:145-148` wraps `<livewire:notification-bell />` inside `@auth … @endauth`, and every route that renders that layout (`/dashboard`, `/indexes`, `/durability` — `routes/web.php:16-36`) sits behind the `auth:sanctum` + `jetstream.auth_session` + `verified` middleware group, so the component can never mount for an unauthenticated actor — matches the "framework already guarantees non-null" exemption in the audit brief, not a gap. The one other `->currentTeam` reference in the codebase, `resources/views/layouts/app.blade.php:131` and `:146` (`Auth::user()->currentTeam->name ?? 'Personal'`), is already null-coalesced. `app/Http/Controllers/Auth/EcosystemAuthController.php:13-31` does not dereference `Auth::user()` at all — it resolves `$user` from an `abort_if`-guarded `PersonalAccessToken::findToken()` lookup (403 on missing/unauthorized/expired token) before ever calling `Auth::login($user)`, so there is no null-actor path there either. **Conclusion: no genuinely reachable unguarded-null-actor bug found — this platform has no admin dashboard/telemetry-viewer component assuming a "current actor," and no background execution surface (job/listener/command) where `Auth::user()` could be silently null.** No code changed, no trait added (per the earlier 0.5.0 pass, this platform correctly has no `HasUserScope`/`HasTeamScope`), no regression test added since there is no reachable defect to regress-test. Verified against a fresh, isolated PostgreSQL 16 database (`dot_memory_test_a1x9`, created and dropped for this pass): `php artisan migrate --no-interaction` (10 migrations, clean) and `php artisan test` — 14 tests, 14 passed, 0 failed, unchanged from the 0.5.0 baseline. |
| 0.5.0 | 2026-08-04 | Platform-loop pass | **Architecture-hardening pass matching the Dot.Finance/Dot.Notify pattern (see Dot.Finance commit `2f75bdb`, Dot.Notify commit `e671436`) — investigated, and deliberately did *not* add a tenant-scope trait.** Read every model in `app/Models` and the `2026_08_01_100002_create_memory_tables` migration: `StorageTier`, `Index`, `RetrievalClass`, `RetrievalObservation`, and `DurabilityOutcome` — Dot.Memory's entire domain schema — have **no `user_id` or `team_id` column at all**, by explicit design (see `Team.php`'s own comment: "Tenant key = owning platform … Team intentionally has no relations into the domain models below", and §7/§2 of this wiki: telemetry is ecosystem-level, not per-team/per-user). This was already independently verified once before, in the 0.2.1 changelog entry, which confirmed `/dashboard`, `/indexes`, and `/durability` are correctly gated only by `auth:sanctum` + `verified` with no team-scoping. Grepping `app/` and `routes/` for `user_id`/`team_id`/`currentTeam` turned up exactly two hits, both Jetstream/Fortify framework code unrelated to the domain models (`CreateNewUser`'s personal-access-token `user_id`, `InviteTeamMember`'s membership `team_id` lookup) — nothing in `app/Http/Controllers/Memory/*` or `app/Livewire/Memory/*` filters by tenant, because there is no tenant column to filter by. `NotificationBell` (the one place per-user scoping legitimately applies — Jetstream's own `notifications` table) was already correctly scoped to `auth()->user()->notifications()` before this pass; left untouched. **Conclusion: applying `HasUserScope` or `HasTeamScope` here would contradict this platform's own documented trust boundary** ("store without reading" / telemetry-plane-not-data-plane, wiki.md §2) by pretending these ecosystem-shared models are per-tenant when they are not — so no trait was created, no model was changed, no controller/Livewire query was touched, and no cross-tenant regression test was added, because there is no tenant boundary here for such a test to prove. This is the intended "skip legitimately global/shared catalog models" outcome the rollout's own instructions called for, not an incomplete pass. Added `phpstan.neon.dist` (level 5, Larastan extension) as dev tooling anyway, matching the rollout's tooling baseline — config present, execution unverified in this sandbox (`vendor/bin/phpstan analyse --memory-limit=1G` produced no stdout/stderr and exit code 1 with no diagnostic, consistent with the same silent-failure behavior recorded on every other platform in this rollout). `composer audit` found 6 pre-existing advisories (1 high — noncanonical-host check bypass, CVE-2026-69246 — and 5 medium) against `guzzlehttp/guzzle`, predating this pass; fixed via `composer update guzzlehttp/guzzle guzzlehttp/psr7 guzzlehttp/promises --with-all-dependencies` (7.12.3 → 7.15.2) — `composer audit` clean afterward. Verified for real against a fresh, isolated `dot_memory_pilot` PostgreSQL 16 database (created and dropped for this pass, not the persistent `dot_memory_verify` db `.env` otherwise points at): `php artisan migrate` (10 migrations, clean) and `php artisan test` both before and after the Guzzle bump — 14 tests, 14 passed, 0 failed, identical counts pre- and post-update, so the dependency bump is behavior-neutral. |
| 0.4.0 | 2026-08-03 | Sakhile Bhayi | Redesigned `resources/views/welcome.blade.php`'s marketing surface: it had shipped as the untouched default Laravel/Jetstream scaffold page (no nav, no hero, no brand mark — just the "Let's get started" documentation card), so this pass builds a full custom marketing page from scratch — nav + hero + features + storage-tiers table + CTA + footer — matching the structural pattern already piloted on `mines`' `welcome.blade.php`. Nav and footer brand marks use the real `public/images/logo.png`. Hero background is a real, licensed Unsplash photo of a server rack (photo by Kevin Ache, @kevinache, unsplash.com/photos/a-rack-of-servers-in-a-server-room-2JJ3wBHu4_0), hotlinked via Unsplash's CDN with a dark gradient overlay tuned for WCAG-adequate text contrast, photographer credited inline as an HTML comment. Copy is drawn only from this wiki's real §1/§3/§5 content (storage tiers, retrieval SLA contracts, forgetting policy) — no fabricated stats or testimonials. The CDN image URL was verified with `curl -sI` before use (HTTP/2 200). |
| 0.1.0 | 2026-08-01 | Memory Platform Lead | Initial wiki: architecture blueprint derived from Dot.Brain's platforms/dot-memory.md and brain.memory.md, adapted to platform-owned framing |
| 0.3.0 | 2026-08-02 | Sakhile Bhayi | **Executed for real, for the first time** — and it did not boot: `storage/framework/{cache,sessions,testing,views}`, `storage/app/{public,private}`, and `bootstrap/cache/.gitignore` were entirely absent from this repo (unlike the platform it was copied from), so nothing had ever created them and `realpath(storage_path('framework/views'))` returned `false`, crashing Laravel's Blade compiler on the very first `composer install`. Fixed by adding the missing directories with their standard `.gitignore` placeholders, matching Dot.Plug's real structure. After the fix: `composer install`, `migrate` (10 migrations, clean), and the full test suite all ran clean — 14 tests, 14 passed, 0 failed, including the "store without reading" invariant tests, now genuinely verified rather than "verified directly against the schema" by review alone. |
| 0.2.0 | 2026-08-01 | Claude Sonnet 5 (AI, hand-authored scaffolding pass) | First code pass: Jetstream Teams shell copied from Dot.Billing's verified boilerplate; domain models/migrations for StorageTier, Index, RetrievalClass, RetrievalObservation, DurabilityOutcome; SLA/index/durability monitoring dashboard; seeder with example telemetry matching §5's targets; structural test asserting no model can hold tenant content. **Entirely unverified** — written without PHP/Composer/PostgreSQL access, so nothing has been installed, migrated, or run. Roadmap items above marked done reflect code written, not code proven working. |
| 0.2.1 | 2026-08-01 | Claude Sonnet 5 (AI, incremental pass) | Re-verified access scoping: `/dashboard`, `/indexes`, `/durability` are correctly gated only by `auth:sanctum` + `verified` (no team-scoping), matching the ecosystem-wide telemetry design in §2/§4 — nothing incorrectly restricts or under-restricts access. Checked every Livewire component (`SlaDashboard`, `IndexInventory`, `DurabilityOutcomes`, `NotificationBell`) for the unchecksummed-argument IDOR pattern found elsewhere this session — none exists; the only record-scoped method (`NotificationBell::markAsRead`) is correctly scoped to `auth()->user()->notifications()`. Re-verified "store without reading": all five domain models' `$fillable` arrays and the `2026_08_01_100002_create_memory_tables` migration still contain zero content-shaped columns, and `StoreWithoutReadingInvariantTest` still covers all five models — the invariant is fully intact. Found and fixed one concrete gap: the dedicated `/indexes` (Index Inventory) and `/durability` (Durability Outcomes) pages existed with real routes, controllers, and views but had no navigation links anywhere (desktop or mobile nav) — reachable only by hand-typing the URL. Added nav links in `resources/views/navigation-menu.blade.php`. |

## Open Questions

- Regulator direct access to `retr:audit:warm`: mediated through the consuming platform, or a standing authenticated read path?
- Cold-tier retention horizons per content class, pending regulatory jurisdiction rules.
